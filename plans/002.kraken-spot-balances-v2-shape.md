# Plan 002 — Kraken spot balances v2 shape

Spec: `specs/002.kraken-spot-balances-v2-shape.md`.

## Solution
Tier 1 touch of `prepareKrakenBalanceMsg` (`src/userStream.ts`): a first branch
for `{data:[…]}` producing `{asset, free}` entries; `locked` becomes optional on
the local entry type. No other code path changes.

## Blast Radius
1. Callers: the Kraken `client.on('message')` handler only (grep). Futures
   branches untouched, pinned by the `holding` test.
2. This repo IS `core/` for `websocket-connector` (cloud) and the self-hosted
   image; the pointer bump in `websocket-connector` is deliberate; the
   userStream process must restart to pick it up.
3. Contract: `OutboundAccountPosition.balances[].locked` becomes optional on
   the wire for Kraken spot. Consumer main-app `core/src/utils/user.ts`
   `processBalanceUpdate` writes `locked: normalizeLocked(parseFloat(d.locked))`
   → `0` when absent; main-app core 1.57.1 changes it to leave `locked` alone
   when the field is absent. Deploy main-app ≥ 2.88.4 (API restart) before or
   with the connector; between the two, Kraken spot rows would show
   `locked = 0` (available overstated) — same as any non-Kraken venue that
   sends no hold figure today.
4. Tests on the path: `test/krakenFillCumulative.test.ts` (same connector
   class), full `npm test` (40).

## Traceability
| Spec | Test |
|---|---|
| §4.1 | `krakenSpotBalances.test.ts` "snapshot …", "update …", "asset names …" |
| §4.2 | "… free only", "… carries the new total as free" (`locked` absent) |
| §4.3 | "futures `holding` messages are unchanged" |
| §4.4 | "an empty spot payload still returns undefined" |
