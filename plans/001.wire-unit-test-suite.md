# 001 — Wire the existing `node:test` suite into CI

## Build order

1. Fix the broken import in `test/hyperliquidFillPark.test.ts` (§4): point
   `ExchangeEnum` at `../src/utils/common` and `CommonOrder` at `../type`,
   matching how `src/utils/hyperliquidFillPark.ts` already imports them.
2. Confirm `npm test` is green locally (all 5 spec files, 26 subtests).
3. Add `.github/workflows/test.yml`: on `pull_request` (and `push` to
   `main`), checkout, `actions/setup-node@v4` with Node 22 (matches the
   Node version already used successfully in local dev and in sibling
   repos' CI — §3.1; not 24, since nothing here requires it beyond one
   dependency's advisory `engines` warning), `npm ci`, `npm test`.

## Contract decisions (§3.1)

Node 22 in CI, not 24 — the `engines` bump in `@nktkas/hyperliquid` is a
warning today, not an enforced floor, and matches what's already proven
to work.

## Blast radius

1. **Other call sites.** The import fix only touches
   `test/hyperliquidFillPark.test.ts`; grepped for
   `core/src/utils/common` and `core/type` elsewhere in this repo — no
   other matches, so no other file shares the broken path.
2. **`core/` submodule.** This repo has no `.gitmodules` — it *is* the
   `core` that `websocket-connector` submodules. Nothing here changes
   what that consumer sees until it bumps its submodule pointer (out of
   scope for this change; that repo gets its own CI/PR).
3. **CLAUDE.md-documented contracts.** No API/queue/event shape changes
   — this is test-file and CI-file only.
4. **Other tests exercising this path.** None beyond the 5 files already
   in `test/*.test.ts`, which is CI's own coverage.

## Traceability

| Spec section | Test / file |
|---|---|
| §2.1 (CI runs `npm test`, fails PR on red) | `.github/workflows/test.yml` |
| §4 (broken import blocks green CI) | `test/hyperliquidFillPark.test.ts` import fix |
